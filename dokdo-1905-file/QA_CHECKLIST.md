# 독도연구소: 1905 파일 — BUILD QA CHECKLIST

이 문서는 `DESIGN_LOCK v1.0`을 바꾸기 위한 문서가 아니라, 현재 BUILD가 잠긴 설계를 실제 브라우저에서 제대로 구현했는지 확인하기 위한 실기 체크리스트다.

## 0. 테스트 원칙

- production `game_results`에 테스트 행을 넣지 않는다.
- 저장 검증은 DevTools fetch intercept, mock, 승인된 test 환경 중 하나를 사용한다.
- 실제 학생 화면에 내부 A/B 등급, source ID, JACAR ref, QA 용어가 튀어나오지 않는지 확인한다.
- 역사 판단을 바꾸지 않고 **화면 이해 가능성 / 동작 가능성 / 상태 보존 / 저장 계약**을 검사한다.

### 자동 런타임 스모크 — 2026-09-14

현재 modular runtime의 상태 전이와 SAVE 계약을 깨지 않고 실행할 수 있는지 확인했다.

- [x] `app-core.js`, `app-scenes-a.js`, `app-scenes-b.js`, `app-init.js`를 각각 `node --check`로 검사했다.
- [x] headless Chromium에서 S0 → S7 → 결과 화면까지 한 경로를 완주했다.
- [x] S2에서 `현재 지명 식별은 보류` 경로로 진행한 뒤 그 상태가 결과 및 구조화 payload에 남는 것을 확인했다.
- [x] Supabase POST를 브라우저 단계에서 intercept하여 production DB에 쓰지 않고 payload를 캡처했다.
- [x] 캡처 payload에 S7 자유서술 제목·본문·주의문 원문이 들어가지 않는 것을 확인했다.
- [x] 실제 branch의 전체 사료 데이터·스타일·원문 링크를 함께 로드한 390 / 768 / 1280 브라우저 회귀를 수행했다. (`Dokdo Draft Browser QA` run #5)
- [x] 390 / 768 / 1280에서 horizontal overflow가 없음을 자동 확인했다.
- [x] 한글 폰트를 설치한 QA runner에서 스크린샷을 다시 캡처해 반응형 UI를 시각 감사했다.

세부 자동 QA 이력은 `QA_BROWSER_REPORT_2026-09-14.md`에 기록한다. 이 자동 검사는 실제 학생·교사의 수업 사용성 검사를 대체하지 않는다.

## 1. 시작·공통 UI

- [x] `/dokdo-1905-file/?class=test` 형태의 직접 진입으로 실제 branch 게임을 로드했다.
- [x] 모바일 390px, 태블릿 768px, 데스크톱 1280px에서 가로 스크롤이 생기지 않는다.
- [ ] 실제 학생이 시작 화면에서 게임 목표를 20초 안에 이해하는지 관찰한다.
- [x] 한국사/동아시아사/독도교육 렌즈가 모바일에서도 정상 배치·선택된다.
- [ ] 최소 VN을 켠 상태에서 학생이 대화를 사료 정답 안내로 받아들이지 않는지 관찰한다.
- [x] VN OFF 상태에서도 핵심 Gameplay 상태 전이가 깨지지 않고 완주된다.
- [ ] 새로고침 후 sessionStorage 복원을 학생 동선별로 수동 확인한다.

## 2. S0 — 전시 초안 검수

목표: 첫 60초 안에 학생이 실제 조작을 시작한다.

- [x] 가공 전시 원고에 `게임 속 가공 원고 / 역사 사료 아님` 표기가 있다.
- [x] 학생이 문제 표현을 직접 선택하는 조작이 작동한다.
- [x] 선택 전에 특정 정답 표현이 자동 선택되어 있지 않다.
- [x] 표현 1개 + 조사 목적 1개를 선택해야 다음으로 진행된다.
- [x] 첫 판단이 이후 결과 화면의 `처음 검수` 기록에 남는다.
- [ ] 실제 학생이 첫 60초 안에 조작을 시작하는지 관찰한다.

## 3. S1 — 1905 내각 결정

목표: 학생이 `판단 근거`와 `실제 행정 결정`을 구분한다.

- [x] 학생용 현대어 재구성과 원문 핵심구절이 별도 정보층으로 표시된다.
- [x] 현재 일본 정부의 `주권 재확인` 설명은 당대 결정문과 별도 선택자료로 존재한다.
- [x] 최소 1개의 판단 근거 + 1개의 결정 구절을 선택해야 진행된다.
- [x] 나카이 청원 / 현재 일본 정부 설명 / 시마네현 고시는 전부 열지 않아도 진행 가능하다.
- [x] 실제로 열람한 보조자료만 `evidence_seen`에 들어간다.
- [ ] 학생이 자료 분량을 과도하다고 느끼지 않는지 수업 사용성에서 확인한다.

## 4. S2 — 1877 태정관 지령

목표: **`본문 직접 문구`와 `현재 지명 식별`을 같은 층위의 사실로 처리하지 않는다.**

현재는 실제 기죽도약도 asset blocker가 남아 있으므로 두 단계로 검사한다.

### DEV placeholder 상태
- [x] 화면에 `DEV PLACEHOLDER`가 명확히 표시되어 최종 사료 이미지로 오인되지 않는다.
- [x] 본문의 `죽도 외 1도` 처리 내용을 먼저 확인한다.
- [x] 지도 단서 2개를 확인하기 전에는 현재 지명 식별 메모 선택이 활성화되지 않는다.
- [x] 지도 단서 확인 뒤 `지도·후대 해석을 연결해 식별`과 `현재 지명 식별 보류`가 모두 선택 가능하다.
- [x] 두 해석 선택에 성공/실패·정오 점수를 붙이지 않는다.
- [x] 힌트를 써도 자동 정답 처리하지 않는다.
- [x] 선택한 해석/보류 상태가 `outside_one_status`로 저장된다.
- [x] 학생 화면은 `磯竹島略圖`를 태정관 최종 지령의 직접 첨부물이라고 단순화하지 않고 `같은 일건에 편철된 시마네현 제출 지도`라고 설명한다.

### 실제 기죽도약도 asset 교체 후
- [ ] 국립공문서관 원 공개본에서 실제 지도 page를 확정한다.
- [ ] 공식 이미지로 local crop을 제작한다.
- [ ] 크롭/확대 등 가공 사실과 출처를 화면에 표시한다.
- [ ] 실제 지도에서 명칭·방향·상대 위치 단서를 클릭/탭할 수 있다.
- [ ] 모바일에서도 hotspot이 44px 이상 터치 가능 영역을 가진다.
- [ ] 지도 이미지를 보지 않고 버튼 문구만 읽어도 식별 결론이 자동 노출되지 않는다.

## 5. S3 — 1900 칙령 제41호

목표: `석도`를 원문 사실과 현재 지명 식별 문제로 분리한다.

- [x] 제1조와 제2조 내용이 학생용 현대어와 원문 핵심구절로 제시된다.
- [x] 원문에는 `석도`라고 표시되고 학생 화면에서 자동으로 `(독도)`를 붙이지 않는다.
- [x] 행정개편과 관할구역 구절을 각각 선택해야 진행된다.
- [x] 장면 종료 시 `현재 독도와의 연결: 추가 근거 필요` 상태가 저장된다.
- [x] 이 메모가 S5에서 실제로 다시 등장한다.
- [ ] 학생이 `추가 근거 필요`를 실패로 받아들이지 않는지 관찰한다.

## 6. S4 — 선택 조사

목표: 모든 자료를 누르는 전자교재가 아니라 필요한 자료를 골라 읽는 조사 경험이 된다.

- [x] 니타카 / 지명연구 / 쓰시마 / 맥락 연표 중 필요한 자료만 열 수 있다.
- [x] 모든 보조자료를 열지 않아도 진행 가능하다.
- [x] 니타카를 열면 `누가 기록 / 정보 경로 / 명칭` 분해 조작이 열린다.
- [x] 니타카 세 요소를 모두 확인한 경우 명칭 근거가 연구 메모에 들어간다.
- [x] 니타카 자료를 `대한제국 공식 명명 문서`로 표현하지 않는다.
- [x] 쓰시마 자료를 열더라도 `러일전쟁 때문에 편입`으로 자동 결론내리지 않는다.
- [x] `직접 원인이라고 주장`을 선택해도 게임오버가 아니라 근거 범위 경고를 준다.
- [x] 선택한 자료만 `evidence_seen` / 연구 보드에 남는다.
- [ ] 실제 학생이 선택 조사라는 사실을 이해하고 `전부 열어야 한다`고 느끼지 않는지 관찰한다.

## 7. S5 — 1906 후속 기록 / Belief Updating

이 장면은 게임의 핵심 유지 조건이다.

- [x] 심흥택 관련 보고에서 `본군 소속 독도`와 `일본 측 편입 통보`를 각각 표시할 수 있다.
- [x] 의정부 지령에서 중앙정부의 대응을 표시할 수 있다.
- [x] 충분한 1906 구절을 표시하기 전에는 1900 메모 수정 버튼이 비활성화되어 있다.
- [x] 새 근거 트레이에 이번 플레이에서 확보한 선택 조사 단서가 함께 표시된다.
- [x] S3의 옛 메모 문장이 그대로 다시 보인다.
- [x] 학생이 `근거 보강 / 판단 보류`를 직접 선택해 과거 메모를 수정한다.
- [x] 어떤 선택에도 `1900년 원문 표기는 석도`라는 주의가 남는다.
- [x] `revision_count`가 실제 수정 행동과 맞게 기록된다.
- [ ] 실제 학생이 `새 자료 때문에 이전 판단을 다시 검토했다`고 인식하는지 관찰한다.

## 8. S6 — 연구 보드

목표: 자유 배치 놀이가 아니라 자료 관계를 판단하는 Gameplay가 된다.

- [x] 실제 열람한 자료만 보드와 선택 목록에 나온다.
- [x] 연도 위치는 고정되어 태블릿 드래그를 요구하지 않는다.
- [x] 핵심 사료는 2~4개만 선택할 수 있다.
- [x] 관계선은 최대 3개만 저장된다.
- [x] 서로 같은 자료를 A/B로 선택하면 추가되지 않는다.
- [x] 관계를 삭제하고 다시 만들 수 있다.
- [x] 최소 핵심자료 수와 최소 관계 수를 충족해야 S7로 진행된다.
- [x] 모바일 체크박스가 텍스트와 같은 행에 정상 배치된다.
- [ ] `보강 / 긴장 / 후속 대응 / 시대적 배경 / 직접 인과 단정 어려움`의 의미를 학생이 설명할 수 있는지 관찰한다.

## 9. S7 — 전시 패널

목표: 미리 준비된 모범답안을 편집하는 것이 아니라 **내가 실제 저장한 메모**를 재료로 쓴다.

- [x] `extractedNotes + boardLinks`에서 전시문 재료가 생성된다.
- [x] 제목·본문·주의문 입력과 결과 전환이 390/768/1280에서 작동한다.
- [x] 자유서술을 서버 저장하지 않는다는 설계가 runtime payload와 일치한다.
- [x] 시스템이 `석도=독도 확정`, `러일전쟁이 유일한 원인` 같은 과잉 문구를 자동 생성하지 않는다.
- [x] 전시 확정 뒤 처음 검수 → 1900 보류 → 1906 수정 궤적이 결과 화면에 보인다.
- [ ] 실제 학생이 메모 블록을 단순 복붙이 아니라 자기 전시 설명의 재료로 사용하는지 관찰한다.

## 10. SAVE payload

브라우저에서 `fetch`를 intercept하여 production DB INSERT 없이 검증했다.

필수 최상위:

```js
{
  class,
  game: 'dokdo-1905-file',
  choices: {...},
  comment,
  timestamp
}
```

`choices`에서 확인:

- [x] `result_id`
- [x] `version`
- [x] `course_lens`
- [x] `evidence_seen`
- [x] `hint_used`
- [x] `outside_one_status`
- [x] `revision_count`
- [x] `seokdo_status`
- [x] `context_relation`
- [x] `board_links`
- [x] `selected_exhibit_sources`
- [x] `completion`

서버에 보내지 않는 값:

- [x] S7 자유서술 제목·본문·주의문 원문
- [x] `openedOptional` 등 세션 UI 복원용 내부 상태

## 11. 접근성 / 반응형

- [x] 보이는 버튼에 접근 가능한 이름이 있다.
- [x] 390 / 768 / 1280 자동 회귀에서 horizontal overflow가 없다.
- [x] 모바일 radio / checkbox의 전역 width 문제를 수정했다.
- [x] 900px 이하에서도 장면 질문·해야 할 일이 연구 노트보다 먼저 나온다.
- [ ] 실제 키보드만으로 전체 장면을 사람이 완주해 focus 순서와 dialog 복귀를 검수한다.
- [ ] 실제 iPad/Chromebook 환경에서 터치 크기와 소프트 키보드 입력을 확인한다.

## 12. 자산 / 출처 blocker

- [x] 일본 국립공문서관 `3018187`과 JACAR `A07060000300`의 문서 관계를 locator 문서에 기록했다.
- [x] `磯竹島略圖` provenance를 `시마네현 제출 지도 / 같은 일건에 편철`로 수정했다.
- [x] 니타카/쓰시마 공식 JACAR 레코드 링크를 학생 자료 카드에 연결했다.
- [x] CI에서 공식 metadata endpoint probe를 실행했고 자동 환경에서는 모두 403임을 기록했다.
- [ ] `磯竹島略圖`의 실제 공식 page / IIIF image identifier를 확정한다.
- [ ] 실제 공식 지도 image로 S2 DEV PLACEHOLDER를 교체한다.
- [ ] `C09050402800` 1904-11-20 기록의 정확한 스캔 프레임 locator를 확정한다.
- [ ] JACAR/NIDS 이미지는 2차 이용 허가 확인 전 저장소에 복제하지 않는다.

## 13. lifecycle gate

현재 자동 branch browser QA는 **통과**했다. 그러나 아래가 남아 있으므로 `draft`를 유지한다.

- S2 실제 기죽도약도 asset/hotspot
- 쓰시마 정확한 frame locator
- 실제 학생/교사 수업 사용성 검수

따라서 이 문서의 체크가 늘어났다는 이유만으로 PR을 merge하거나 `implemented/tested/published`로 승격하지 않는다.
