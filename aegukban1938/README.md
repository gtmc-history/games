# 열 집을 묶다 — DESIGN_LOCK

- Canonical slug / `game_id`: `aegukban1938`
- 학생용 탐구 중 제목: 「열 집을 묶다」
- 결과 공개 제목: 「열 집을 묶다 — 애국반과 전시 동원 체제」
- 설계 기준: 첨부 A~E v1.1.1 (확정일 2026-08-19)
- 구현 상태: A를 최상위 규칙으로, C를 상태전이·밸런스의 source of truth로, B를 가구 데이터로, D를 학생 화면 문안으로, E를 구현·검증 명세로 사용한다.
- SAVE: `required`
- DASH: `A`
- Renderer: `generic`

## 잠긴 핵심

이 게임은 실제 애국반 행정 절차를 재현하지 않는다. 교과서와 사료가 확인해 주는 요소로 정보–생활 통제–동원의 연결 구조를 모델링한다. 학생의 행위자성은 제한된 확인 자원의 배분이며, 복종·저항이나 반장의 선악을 판정하지 않는다.

진행은 `intro → r1 → r2 → r3 → r4 → result`의 6장면으로 고정한다. 랜덤, 뒤로 가기, 확정 후 선택 변경, 점수·승패·등급, R4의 선택 UI는 없다. R3 자원은 모든 경로에서 6회이며 이월하지 않는다. R4 조회는 `members + age[1] + age[9] + school[2] + register[3] + register[8] + register[9]`의 7건이다. `ssi`는 R3 전용 확인 상태이며 R4 판정에 사용하지 않는다.

## Codex 질문 로그

(비어 있음)
